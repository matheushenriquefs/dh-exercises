// Agora, vamos criar 3 variáveis numéricas mas desta vez usando a função nativa Number(). Eles devem ser chamados positivos, 
// negativos e decimais, e cada uma delas deve armazenar um número que corresponda ao seu nome.

let positivo = Number(1);
let negativo = Number(-1);
let decimal = Number(1.1);