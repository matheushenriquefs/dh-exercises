// A variável nan você atribui a ele o que é devolvido pelo parseInt() quando passamos um dado não-numérica
// A variável correto você atribui a ele o que é devolvido pelo parseInt() quando passamos um dado numérico

const nan = parseInt("text");
const correto = parseInt(1.1);