// Declare uma variável com o nome dado e atribua um valor booleano.
// você deve fazer um  if para avaliar essa variável que se for true imprimir "É verdadeiro!", caso contrário "É falso!".

let dado = true;
function comparaBooleano(bool){
    if(!bool){
        return "É falso!";
    }else{
        return "É verdadeiro!";
    }
}