// criar uma função chamada "substituicaoRapida" que você recebe um texto, uma palavra para procurar e a palavra que substituirá a 
// que você está procurando. A função deve retornar o texto com a palavra substituída.

function substituicaoRapida(texto, procurar, substituir){
    return texto.replace(procurar, substituir);
}